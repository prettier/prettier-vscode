pre-commit:
  parallel: true
  commands:
    prettier:
      glob: "*.{ts,json,md,yml,hbs,js,mjs}"
      run: pnpm prettier --write {staged_files}
      stage_fixed: true
    lint:
      glob: "src/**/*.ts"
      run: pnpm lint
