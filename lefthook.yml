pre-commit:
  parallel: true
  commands:
    prettier:
      glob: "**/*.{ts,json,md,yml,hbs,js,mjs}"
      run: npm run prettier -- --write {staged_files}
      stage_fixed: true
    lint:
      stage_fixed: true
      glob: "src/**/*.ts"
      run: npm run lint
